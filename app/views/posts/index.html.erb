<h3><%= t("pages.allarticles") %></h3>
<% @post.each do |post| %>
  <div class = "alert alert-light">
    <h3><%= post.title %></h3>
    <p><%= post.body %></p>
    <%= link_to t("pages.learnmore"), post_path(post), :class => 'btn btn-outline-primary' %>
  </div>
<% end %>
